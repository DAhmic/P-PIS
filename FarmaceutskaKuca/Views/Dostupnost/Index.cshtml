@model IEnumerable<FarmaceutskaKuca.Models.dostupnost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.procenat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.servis.naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mjesec.naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.procenat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.servis.naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mjesec.naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Chart", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
