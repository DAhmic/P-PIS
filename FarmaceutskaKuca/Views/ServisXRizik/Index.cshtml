@model IEnumerable<FarmaceutskaKuca.Models.servisxrizik>

@{
    ViewBag.Title = "Index";
}

<h2>Procjena rizika</h2>

<p>
    @Html.ActionLink("Kreiraj novu", "Create")
</p>
<table class="table">
    <tr>
        <!--<th>
            Html.DisplayNameFor(model => model.naziv)
        </th> -->
        <th>
            @Html.DisplayName("Servis")
        </th>
        <th>
            @Html.DisplayName("Rizik")
        </th>
        <th>
            @Html.DisplayName("Procenat")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <!--<td>
            Html.DisplayFor(modelItem => item.naziv)
        </td> -->
        <td>
            @Html.DisplayFor(modelItem => item.servis.naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rizik.naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procenat)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.id }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.id }) |
            @Html.ActionLink("Izbriši", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
